<Window x:Class="StarRuptureSync.Views.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:model="clr-namespace:StarRuptureSync.Models"
        Title="Repository history"
        Height="600" Width="720"
        Style="{StaticResource AppWindow}"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Commit history (newest first)" FontSize="16" FontWeight="Bold"
                   Margin="0,0,0,10" />

        <Border Grid.Row="1" BorderBrush="{StaticResource Stroke}" BorderThickness="1" CornerRadius="6">
            <ListView ItemsSource="{Binding Commits}"
                      SelectedItem="{Binding SelectedCommit}"
                      BorderThickness="0" Background="Transparent"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="10,8" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type model:CommitInfo}">
                        <StackPanel>
                            <TextBlock Text="{Binding Message}" Foreground="{StaticResource Text}"
                                       FontWeight="SemiBold" />
                            <TextBlock Text="{Binding Subtitle}" Foreground="{StaticResource TextDim}"
                                       FontSize="11" Margin="0,2,0,0" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <!-- Restore bar -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="12" Margin="0,12,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                           Text="Restore a session from the selected commit" FontWeight="SemiBold" />

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Session in commit:"
                           Foreground="{StaticResource Text}" Margin="0,10,0,4" />
                <ListBox Grid.Row="2" Grid.Column="0"
                         ItemsSource="{Binding SessionsInSelectedCommit}"
                         SelectedItem="{Binding SelectedSessionName}"
                         MaxHeight="120" Margin="0,0,12,0"
                         Background="{StaticResource PanelAlt}"
                         Foreground="{StaticResource Text}"
                         BorderBrush="{StaticResource Stroke}" BorderThickness="1">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Foreground" Value="{StaticResource Text}" />
                            <Setter Property="Padding" Value="8,5" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="b" Background="Transparent"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="b" Property="Background" Value="#3A3D49" />
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="b" Property="Background" Value="#2E3550" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>

                <Button Grid.Row="2" Grid.Column="1" Content="⤓  Restore into Steam folder"
                        Style="{StaticResource AccentButton}"
                        VerticalAlignment="Bottom"
                        Command="{Binding RestoreCommand}" />

                <TextBlock Grid.Row="3" Grid.ColumnSpan="2" Text="{Binding StatusText}"
                           Foreground="{StaticResource Text}" Margin="0,10,0,0" TextWrapping="Wrap" />
            </Grid>
        </Border>

        <Button Grid.Row="3" Content="Close" HorizontalAlignment="Right" MinWidth="100"
                Margin="0,12,0,0" IsCancel="True" Click="OnCloseClick" />
    </Grid>
</Window>
